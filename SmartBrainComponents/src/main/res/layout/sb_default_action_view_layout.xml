<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/actionView"
    android:background="#6B80F8"
    android:layout_height="65dp">

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/nextButtonTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:textAllCaps="false"
        android:textColor="#FFFCFC"
        android:textSize="16sp" />


    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/nextButtonArrowImageView"
        android:layout_marginStart="5dp"
        android:layout_marginTop="2dp"
        android:layout_width="wrap_content"
        android:background="@drawable/ic_arrow"
        android:backgroundTint="#FFFFFF"
        app:layout_constraintStart_toEndOf="@id/nextButtonTextView"
        app:layout_constraintBottom_toBottomOf="@id/nextButtonTextView"
        app:layout_constraintTop_toTopOf="@id/nextButtonTextView"
        android:layout_height="wrap_content"/>

</androidx.constraintlayout.widget.ConstraintLayout>